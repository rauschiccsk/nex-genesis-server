unit NexConst;

interface

uses Windows, SysUtils;

const
  SUPPLIER_NONE = 0;
  SUPPLIER_CONTI = 1;   // Dodávate¾ je Continental Barum s.r.o. (544)
  SUPPLIER_GOODY = 2;   // Dodávate¾ je Goodyear Slovakia s.r.o. (541)
  SUPPLIER_MARSO = 3;   // Dodávate¾ je Marso Kft. Ntyiregyháza (6690)
  SUPPLIER_IHLE = 4;    // Dodávate¾ je IHLE SLOVAKIA s.r.o.    (1241)

  CODETYPE_BARCODE = 1; // Èiarový kód
  CODETYPE_ORDCODE = 2; // Objednávací kód dodávate¾a

implementation

end.


